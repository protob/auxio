// Makes res.json() and JSON.parse return unknown instead of any, so a new
// apiFetch endpoint physically can't skip its schema.
import '@total-typescript/ts-reset'
import '@total-typescript/ts-reset/dom'
